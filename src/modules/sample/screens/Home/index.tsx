import { App } from '../../components/App'
import { BUNDLE_IDENTIFIER } from 'resources/constants';

export const Home = {
    name: `${BUNDLE_IDENTIFIER}.HOME`,
    screen: `${BUNDLE_IDENTIFIER}.HOME`,
    component: App,
    title: 'Home Screen'
}